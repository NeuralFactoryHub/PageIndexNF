---
name: project-init
description: Scan codebase and generate .claude/project.yml with service discovery, dependency inference, and integration detection.
---

Use the project-init skill exactly as written to scan this codebase and generate (or update) `.claude/project.yml`.
